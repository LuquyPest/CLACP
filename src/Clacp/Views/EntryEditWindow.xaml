<Window x:Class="Clacp.Views.EntryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Entree" Height="480" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Titre" Margin="0,0,0,4"/>
        <TextBox x:Name="TitleBox" Grid.Row="1" Padding="6" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Nom d'utilisateur" Margin="0,0,0,4"/>
        <TextBox x:Name="UsernameBox" Grid.Row="3" Padding="6" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="Mot de passe" Margin="0,0,0,4"/>
        <Grid Grid.Row="5" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <PasswordBox x:Name="PasswordBoxHidden" Grid.Column="0" Padding="6"/>
            <TextBox x:Name="PasswordBoxVisible" Grid.Column="0" Padding="6" Visibility="Collapsed"/>
            <ToggleButton x:Name="ShowPasswordToggle" Grid.Column="1" Content="Afficher" Margin="6,0,0,0" Padding="8,6" Checked="OnShowPasswordChanged" Unchecked="OnShowPasswordChanged"/>
            <Button x:Name="GenerateButton" Grid.Column="2" Content="Generer" Margin="6,0,0,0" Padding="8,6" Click="OnGenerateClick"/>
        </Grid>

        <TextBlock Grid.Row="6" Text="URL" Margin="0,0,0,4"/>
        <TextBox x:Name="UrlBox" Grid.Row="7" Padding="6" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="8" Text="Sequence de frappe automatique" Margin="0,0,0,4"/>
        <ComboBox x:Name="AutoTypeCombo" Grid.Row="9" Padding="6" Margin="0,0,0,10">
            <ComboBoxItem Content="Utilisateur puis Tab puis mot de passe"/>
            <ComboBoxItem Content="Mot de passe uniquement"/>
        </ComboBox>

        <TextBlock Grid.Row="10" Text="Notes" Margin="0,0,0,4" VerticalAlignment="Top"/>
        <TextBox x:Name="NotesBox" Grid.Row="11" Padding="6" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="12" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="CancelButton" Content="Annuler" Padding="10,6" Margin="0,0,8,0" Click="OnCancelClick"/>
            <Button x:Name="SaveButton" Content="Enregistrer" Padding="10,6" Click="OnSaveClick"/>
        </StackPanel>
    </Grid>
</Window>
